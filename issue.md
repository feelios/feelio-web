# 추가 이슈 등록

- [ ] [UX/UI] AI 분석 레이아웃 글자 깨짐 및 짤림 현상 수정
  - 모바일에서 긴 글 줄바꿈 보정 및 웹 환경에서 카드 멘트가 짤리는 현상을 수정합니다.
- [ ] [UX/UI] 홈 화면 말랑이 빈 상태(물음표) 렌더링 로직 개선
  - 전체 거래내역이 없을 때만 물음표 노출하고, 이번 달 데이터 부재 시 저번 달 대표 감정으로 렌더링합니다.
- [ ] [Refactor] 지출/수입 폼 감정 리스트 하드코딩 전환 (API 최적화)
  - 지출/수입 등록용 불변 감정 데이터를 프론트 단에 하드코딩하여 API 불필요한 로딩을 제거합니다.
- [ ] [UX/UI] 캘린더 날짜 선택 시 감정 색상 연동 및 그림자 효과 추가
  - 달력에 선택된 감정 색상을 적용하고, 감정 미선택 시에는 배경색 없이 그림자 효과(Stroke & Shadow)만 렌더링합니다.
- [ ] [UX/UI] 캘린더 날짜 및 시간 뷰(모드) 선택 UI 분리
  - 날짜 클릭 시 캘린더(날짜 뷰)만 띄우고, 시간 클릭 시 휠(시간 뷰)만 별도로 노출하도록 UI를 개편합니다.
- [ ] [Backend] AI 팩트 리포트 프롬프트 조정 (강력한 경고 톤)
  - 팩트 리포트를 생성하는 AI 프롬프트를 수정하여, 지출 위험에 대해 '이대로 가면 파산이다'와 같이 훨씬 더 강력하고 직관적인 경각심을 주도록 어조를 변경합니다.
- [ ] [Backend] 홈 화면 말랑이 코멘트용 AI 생성 API 구현
  - 홈 말랑이 코멘트를 위한 프롬프트를 추가합니다. 수치를 포함한 평가(칭찬/경고)와 독려 멘트를 생성하여 프론트엔드로 반환하는 API를 개발합니다.
- [ ] [Backend] 거래내역 생성 및 수정 시 시간(Time) 바인딩 버그 수정
  - 거래내역을 생성하거나 수정할 때, 등록 시간(createdAt)과 수정 시간(updatedAt) 중 올바른 시간이 DB에 반영되고 조회 시 정확히 반환되는지 점검 및 수정합니다.
- [ ] [UX/UI] 모바일 AI 대시보드 위험/팩트/챌린지 카드 아코디언(접기) 적용
  - 모바일 화면에서는 클릭하기 전에 카드의 제목(Title)만 보이고, 누르면 상세 내용이 펼쳐지도록(Accordion/Toggle) UX를 수정합니다.
- [ ] [UX/UI] 웹 환경 AI 감정소비 분석 카드 텍스트 짤림/깨짐 수정
  - 웹(데스크탑) 환경에서 글자 내용이 많을 때 AI 감정 카드의 멘트가 영역 밖으로 넘치거나 깨지는 CSS 레이아웃 이슈를 해결합니다.
- [ ] [UX/UI] 목표 예산 현황 카드 내 감정 말랑이 UI 재배치
  - 예산 카테고리 '아래'에 뜨던 감정 텍스트를 카테고리 '옆'으로 이동시키고, 텍스트 대신 해당하는 감정의 말랑이 이미지(아이콘)를 렌더링하도록 UI를 개편합니다.
- [ ] [UX/UI] 홈 화면 말랑이 코멘트 AI 연동 및 데이터 부재 고정 문구 처리
  - AI API를 연동하여 3줄 멘트(시작, 평가, 독려)를 바인딩합니다. 데이터가 아예 없는 최초 상태에서는 '달마다의 기록으로 말랑이가 바껴요' 안내 문구를 노출합니다.
- [ ] [UX/UI] 목표 100% 달성 시 홈 카드 및 설정 모달 완료 디자인 수정
  - 목표 완수(100%) 시 홈 화면의 목표 카드와 설정 모달 내부에서 보여지는 시각적 완료 상태 디자인(UI/UX)을 개선합니다.
- [ ] [UX/UI] 나의 소비 코어 탭 클릭 시 레이아웃 흔들림(Shift) 고정
  - 소비 코어 카드에서 내부 탭 버튼들을 누를 때마다 버튼들의 위치가 미세하게 움직이거나 덜컹거리는 레이아웃 버그를 CSS로 단단히 고정합니다.
- [ ] [UX/UI] 거래내역 상세/수정 모달 시간(Time) 바인딩 프론트 로직 점검
  - 백엔드에서 넘어온 올바른 시간 데이터를 프론트엔드의 상세 보기 모달과 수정 달력에 정확하게 파싱하고 연결하는지 확인 후 방어 로직을 작성합니다.
- [ ] [UX/UI] 로그아웃 수행 시 활성화된 모달 닫기 처리
  - 유저가 로그아웃을 진행하여 라우팅이 변경될 때, 띄워져 있던 모달이나 팝업 창 상태를 강제로 닫기(Close) 처리하여 잔상 버그를 방지합니다.
- [ ] [Backend] 회원 탈퇴 API(Delete User) 오류 원인 분석 및 수정
  - 현재 계정 탈퇴가 정상적으로 처리되지 않는 백엔드 원인을 파악하여, 회원의 모든 데이터가 안전하게 삭제(또는 비활성화)되도록 API 로직을 수정합니다.
- [ ] [UX/UI] 데스크탑 웹 사이드바 상단 로고 이미지 교체
  - 전달된 새로운 'Feelio 말랑이' 로고 에셋을 추가하고, 데스크탑 웹 환경 사이드바 상단의 기존 이미지를 해당 로고로 교체합니다.
- [ ] [UX/UI] 회원 탈퇴 프론트엔드 연동 오류 확인 및 상태 초기화 처리
  - 회원 탈퇴 API 호출 시 네트워크 오류를 점검/수정하고, 탈퇴 성공 시 로컬 스토리지 및 세션을 완벽히 초기화하여 로그인 화면으로 리다이렉트되도록 방어 로직을 추가합니다.
